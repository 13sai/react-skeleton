import { Setting } from '@/types/settings';

const settings: Setting = {
  siteTitle: 'react-skeleton',
  siteTokenKey: '13sai',
  tokenKey: 'token',
  noAuthUrl: ['/user/login', '/user/info'],
  headFixed: true,
  theme: 'light',
  leftSidebar: true,
  tabNavEnable: true,
  tabNavHomePath: '/home/workspace',
  navMode: 'inline',
};

export default settings;
